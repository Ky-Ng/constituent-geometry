# Experiments

Each numbered folder is a self-contained experiment with its own `run.py`, results, and logs.

| # | Name | Status | Description |
|---|------|--------|-------------|
| 00 | `00_example` | Template | End-to-end sanity check of the project setup |
| 01 | `01_build_simple_hi_hf_ds` | Done | Enumerate the depth-3 HI/HF toy-CFG dataset |
| 02 | `02_train_vaswani_simple_hi_hf_ds_depth_3` | Done (caveats) | Train on a random split; ~0.003 loss, but i.i.d. eval can't show generalization |
| 03 | `03_holdout_depth_2_vaswani_hi_hf` | Done | Depth-holdout (train {0,1}, test depth 2), shrunk model (1 head / 2 layers) — underfit |
| 04 | `04_holdout_depth_2_vaswani_hi_hf_heads_4_layers_4` | Done | Capacity control for 03 (4 heads / 4 layers, same small width). Solves depth {0,1} (exact-match 1.0) but fails to generalize to depth 2 (exact-match 0.0, token-acc 0.47) — learned a non-recursive solution |
| 05 | `05_build_dataset_with_frames` | Ready | Frame-based HI/HF dataset builder for the new sub-categorized grammar (~3.3B sentences at depth 3 was infeasible; this enumerates only 90 frames and samples K lexical fillings each). Supports `random_frame` and `held_out_depth` structural splits over frames, not rows. |
| 06 | `06_vaswani_original_hi_hf_frames_heads_4_layers_4_random` | Ready | First training run on the frame-based dataset (experiment 05, `random_frame` split). Original Vaswani encoder-decoder at paper-ish width (`d_model=128, d_ff=512`), shape `n_heads=4, n_layers=4`. Loads a Hub dataset id (default `kylelovesllms/hi_hf_frames_d3_random_100`). |
| 07 | `07_vaswani_RoPE_hi_hf_frames_heads_4_layers_4` | Ready | One-knob ablation of 06: same data / width / shape / optimizer, but the fixed sinusoidal positional table is replaced by RoPE applied to Q/K inside self-attention (cross-attention un-rotated). Same three runs as 06 (`random_depth_3`, `heldoutdepth_3`, `heldoutdepth_4`). |
| 08 | `08_GPT2_RoPE_hi_hf_frames_heads_4_layers_4` | Ready | **New baseline (not a one-knob ablation of 07).** GPT2-style decoder-only Transformer (pre-LN, GELU, biases, tied LM head) with RoPE on Q/K; absolute PE removed. Trains on a single concatenated stream `<bos> hi <sot> hf <eos>` with masked causal-LM loss only on positions after `<sot>` (inclusive of `<eos>`). Same three frame splits as 06/07. Uses a separate tokenizer that adds `<sot>` at id=4. |
| 09 | `09_multi_seed` | Done | Multi-seed reproduction of 06/07/08 (seeds 42–51, `random_frame` depth-3, 30 epochs) testing whether single-seed attention heatmaps are seed-robust. **Result:** all 3 archs saturate test exact-match (~1.0, seed-robust), but cross-seed heatmaps are only *moderately* consistent (matched cosine: vaswani 0.84 > vaswani_rope 0.75 > gpt2_rope 0.66) — a single-seed heatmap is not fully representative. Two-stage CPU heatmap pipeline (`extract_attention.py` → `gather_heatmaps.py`). |
| 10 | `10_multi_seed_grokking` | Done | Grokking probe on **`held_out_depth` depth-3** (real gap), **3000 epochs**, **5 seeds (42–46)**, **wd=0.1**. **Result: no grokking** — all 15 runs memorize (`train_loss→0`) then *overfit* (U-shaped eval_loss climbing to 3–6), test exact-match **0.0 across all archs/seeds**; only `vaswani` flickers to a transient 0.05–0.14. Clean negative result; sets up the planned WD sweep ({0.01, 1.0}). Local `runners/` copies of 06/07/08 add `--weight-decay`. |
| 11 | `11_reproduce_dataset` | Done | Deterministically regenerate `kylelovesllms/hi_hf_frames_d3_random_100` locally (`--split-seed 0`, no `--push`) and verify byte-identical to the Hub. **Result: `ALL SPLITS IDENTICAL ✅`** — train 7,200 / val 864 / test 900 rows, 72/9/9 depth-3 frames, all per-split row/column/content/`frame_id` checks PASS. Confirms the 06–09 training splits are reproducible; writes `split_manifest.json`. |
| 12 | `12_smaller_model` | Done | Capacity ablation of 09 (same seeds/data/30 epochs) at **smaller shapes L3/H2 and L2/H2** (vs 09's L4/H4). **Result:** both Vaswani encoder-decoders stay saturated (~1.0) even at L2/H2, but decoder-only `gpt2_rope` is capacity/seed-fragile (mean 0.94 at L3/H2, **0.70 at L2/H2**, one seed 0.41). Heatmap consistency declines as models shrink (vaswani 0.84→0.79→0.75). Shells out to 06/07/08 with `--n-layers/--n-heads`; paths encode `L<l>_H<h>`. |
| 13 | `13_adjunction_rel_clause_grammar` | In Progress | Grammar v2 dataset builder: adds relative clauses (CP_rel, subject/object-gap) and adjunction (AdjP, AdvP) to the grammar. New columns: position-tagged frames, all-CP depth, tree height, is_ambiguous. Uses `src/grammar/v2/`. |
| 14 | `14_multi_seed_grammar_v2_depth_1` | In Progress | Multi-seed sweep (seeds 42–51) of all 3 architectures on the v2 grammar depth-1 dataset (`kylelovesllms/hi-hf-v2-frames-d1-k100`). All 3 archs saturate test exact-match (~0.999) within the first epoch — depth-1 is substantially easier than depth-3. |
| 15 | `15_multi_seed_grammar_v2_depth_2_random` | Done | Multi-seed sweep (seeds 42–51) of all 3 architectures on the v2 grammar depth-2 random-frame dataset (`kylelovesllms/hi-hf-v2-frames-d2-k100-random`), using the **137-token v2 tokenizer** (adjectives + adverbs in-vocab) and **early stopping** 1 epoch after eval exact-match ≥ 0.98. **Result:** all 30 runs reach test exact-match **0.984–0.999**. Convergence gap by positional encoding — **vaswani (sinusoidal) crosses 0.98 ~epoch 1.06**, both **RoPE variants ~epoch 0.70** (see `figures/architecture_vs_convergence.png`). wandb tag `v2`. |
| 16 | `16_multi_seed_withold_depth2_grok` | Pending | Exp 10's **grokking probe** ported to the **v2 grammar's held-out-depth-2** split (`kylelovesllms/hi-hf-v2-frames-depth_withhold2_k_train100_k_eval5_eval_frame_cap500`: train ~250k shallow trees, val/test 1,250 each deeper/held-out). Byte-identical exp-10 runners (`--weight-decay`/`--lr-scheduler`, **no early-stop**) + the **v2 tokenizers**; constant LR. 3 archs × 5 seeds (42–46), **wd=1.0** (exp 10's wd=0.1 failed to grok on the v1 analog), 50 epochs (~195k steps), `--eval-steps 1000`/`--save-steps 10000`. Looks for `train_loss→0` early vs delayed held-out `eval_exact_match` rise. Adds `--resume` (per-run checkpoint auto-resume for preemption recovery). wandb tag `16_multi_seed_withold_depth2_grok`. |
| 17 | `17_relative_clause_generalization` | In Progress | Subject-position generalization dataset on grammar v2. Training has **atomic subjects everywhere** (proper name or bare `D N`); objects may carry adjectives/relative clauses. Held-out val/test = frames where **some subject is modified** (adj / RC / both). Tests whether adjunction+relativization learned on objects transfers to the subject position. **Grammar v2 is unmodified** — the restriction is a split predicate (`frame_has_branching_subject`, `--split-policy held_out_subject_branching`) pinned to the experiment's `run.py`, mirroring exp 13's `held_out_depth`. New columns `subject_branching`/`subject_mod`. |
| 18 | `18_multi_seed_random_depth2_grok` | Pending | **In-distribution control for exp 16.** Exp 16's grokking-probe machinery (byte-identical runners, v2 tokenizers, 3 archs × 5 seeds 42–46, wd=1.0, constant LR, no early-stop) on the v2 grammar's **random-frame depth-2** split (`kylelovesllms/hi-hf-v2-frames-1.3M-d2-k2-random`: train 2,229,638 / val 278,704 / test 278,706, no structural gap) instead of 16's held-out-depth split. Default **`--epochs 6`** (~209k steps) matches 16's ~195k-step horizon since this train split is ~9× larger; step-based eval/save cadence unchanged. wandb tag `18_multi_seed_random_depth2_grok`. |
| 19 | `19_NP_adjunct_generalization` | Pending | **Position-transfer analog of exp 16's depth gap.** Same grokking-probe machinery (byte-identical 16/18 runners, v2 tokenizers, 3 archs × 5 seeds 42–46, wd=1.0, constant LR, no early-stop) on a v2 split that holds out **relative clauses on the subject** (`kylelovesllms/hi-hf-v2-frames-object-rc-only-depth2`: train 3,936 with atomic subjects / every adjunct on an object; val/test 300 each where the **subject carries an RC**). Tests whether an NP adjunct learned on objects transfers to the subject position (depth-2 take on exp 17). Default **`--epochs 3000`** (~186k steps) matched to exp 10 since this train split is tiny (~62 steps/epoch); step-based eval/save cadence unchanged. wandb tag `19_NP_adjunct_generalization`. |
| 20 | `20_multi_seed_random_depth2_small_models` | Pending | **Shrunk single-epoch variant of exp 18.** Same dataset (`kylelovesllms/hi-hf-v2-frames-1.3M-d2-k2-random`), archs, seeds (42–46), tokenizers, wd=1.0 / constant LR / no early-stop, and step-based eval cadence as 18, but **`--epochs 1`** and a **layer/head capacity sweep**: 4 configs (`L2H2`, `L2H1`, `L3H2`, `L3H1`) threaded to the byte-identical 18 runners via `--n-layers`/`--n-heads`. Full sweep = 3 archs × 4 configs × 5 seeds = **60 runs**; submitted as **one packed job per (arch, config) = 12 jobs**. All eval **during training is capped** to a fixed 2,500-row val/test subset (`--max-eval-samples 2500`) so the 60 runs finish fast; the full-set pass was **removed from the runners** and moved to a separate post-hoc script `run_full_evaluate.py` that scores each saved `best/` checkpoint on the **entire 278,704-row validation set** (reusing the runners' eval machinery). wandb tag `20_multi_seed_random_depth2_small_models`. |
| 21 | `21_causal_tracing_ablations` | In Progress | **Causal tracing / activation patching** on a trained Vaswani seq2seq: which input tokens does the prediction of the *first content noun* depend on? Zero-ablates one input **token embedding** at a time (positional encoding kept) and records `Δ logit(target)` at the decoder step that predicts it. Sweeps the encoder (source/HI) and the decoder prefix via a transient `embed_tokens` swap on the chosen stack (no `inputs_embeds` path exists). Smoke test on the running example (predict "dog"): zeroing source `dog` ⇒ **−14.9 logit**, while the matrix verb `chases` and everything after it (`this researcher`, `<eos>`) ⇒ ≈0 — supporting "tokens past the matrix verb don't matter." |

## Convention
- Create new numbered folders (`01_xxx/`, `02_xxx/`, ...) for new experiments — don't edit old ones.
- Each folder contains: `run.py`, `results/`, `logs/`, `figures/`, and `README.md` (observations).
- This README should only contain brief descriptions of each experiment. Detailed setup, results, and observations belong in each experiment's own `README.md`.
- The `artifacts/` folder should contain large blob files (e.g. `.safetensors` and `.pt` files which should be uploaded to HuggingFace instead of git)
- During a new experiment, you should `cp` the `00_example` folder and rewrite over it